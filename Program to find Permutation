#include<stdio.h>
int main()
{
int n,r,p;
int factorial(int);
scanf("%d",&n);
scanf("%d",&r);
p=factorial(n)/factorial(n-r);
printf("%d",p);
return(0);
}
int factorial(int n){
int i,value=1;
for(i=1;i<=n;i++)
{
	value=value*i;
}
return(value);
}
