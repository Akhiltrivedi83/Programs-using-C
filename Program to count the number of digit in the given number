#include <stdio.h>

int main()
{
    int num,count=0,r;
    printf("Enter the number");
    scanf("%d",&num);
    while(num>0)
    {
    r=num%10;
    num=num/10;
    count=count+1;
    }
    printf("%d",count);
    return 0;
}
