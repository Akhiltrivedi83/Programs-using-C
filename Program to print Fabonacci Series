#include<stdio.h>
int main()
{
    int a=0,b=1,i=0,sum=0,n=10;
    printf("%d ",a);
    printf("%d ",b);
    while(i<n)
    {
    sum=a+b;
    printf("%d ",sum);
    a=b;
    b=sum;
    i++;
    }
    return(0);
}
